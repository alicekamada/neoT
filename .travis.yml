language: node_js
node_js:
  - "node"
before_install:
  - "rm -rf ./*" # geting rid of first cloned repo
  - "git clone https://github.com/alicekamada/neoT.git"
  - "git fetch origin"
  - "git branch -v"
  - "git remote -v"
  - "git checkout testing"
  - "git checkout master"
  - "git merge testing"
